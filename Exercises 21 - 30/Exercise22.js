// Question 22: Create a program that displays the multiplication table for a given number.

let tableNum = 7;
console.log(`Multiplication table for ${tableNum}`);

for (let i = 1; i <= 10; i++) {
  console.log(`${tableNum} x ${i} = ${tableNum * i}`);
}